export default class Filter{
    base: number;
    percent: number;
    year: number;

    constructor(base: number, percent: number, year: number){
        this.base = base;
        this.percent = percent;
        this.year = year
    }
}